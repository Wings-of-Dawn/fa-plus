Update CSS to fix layout of added action-buttons at top and bottom of submissions page
Update JS to make 'check *', 'open checked', and 'open all' buttons work again
Verify that image-centering content script still works
DONE - Change 'cancel' icon to use an admonishment rather than a cross
